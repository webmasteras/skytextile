"use client";

import styled from "@emotion/styled";

const MainHead = styled.span((props) => ({
  fontSize: "1.5rem",
  color: "#717171",
}));

const InnerContainerHead = styled.span((props) => ({
  fontSize: "0.9rem",
  color: "#333",
  margin: "15px",
}));

export { MainHead, InnerContainerHead };
